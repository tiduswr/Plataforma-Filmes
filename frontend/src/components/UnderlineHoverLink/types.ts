import { ReactNode } from "react";

type UnderlineHoverLinkProps = {
  title: string;
  icon: ReactNode;
  prepEndIcon?: boolean;
  hoverColor?: string;
  desc?: string;
};

export type { UnderlineHoverLinkProps };
